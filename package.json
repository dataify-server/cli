{
  "name": "dataify-mcp-cli",
  "version": "0.1.50",
  "description": "Cross-platform CLI for calling Dataify MCP API tools.",
  "type": "module",
  "bin": {
    "dataify": "bin/dataify-mcp.js"
  },
  "files": [
    "bin",
    "src",
    "NPM_USAGE.md",
    "README.md"
  ],
  "scripts": {
    "check": "node --check bin/dataify-mcp.js && node --check src/args.js && node --check src/cli.js && node --check src/client.js && node --check src/config.js && node --check src/output.js && node --check src/repl.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "keywords": [
    "dataify",
    "mcp",
    "cli"
  ],
  "license": "MIT"
}
